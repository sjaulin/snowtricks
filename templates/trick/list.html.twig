{% extends "base.html.twig" %}

{% block title %}|
	{{ slug }}
{% endblock %}

{% block body %}

	<div class="container">

		<section>
			<ol class="breadcrumb">
				<li class="breadcrumb-item">
					<a href="{{ path('home')}}">Accueil</a>
				</li>
				<li class="breadcrumb-item">
					<a href="{{ path('trick_list', {'slug' : category.slug})}}">{{ category.name }}</a>
				</li>
			</ol>

			<h1>{{ category.name }}</h1>

			<div class="row">
				{% include "trick/_list.html.twig" %}
			</div>

		</section>

	</div>


{% endblock %}
